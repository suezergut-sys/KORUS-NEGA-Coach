# Полигон — голосовой тренажёр переговоров

Прототип русскоязычного тренажёра управленческих переговоров. Его первая задача — проверить, насколько естественно и быстро работает двусторонний голосовой диалог через OpenAI Realtime API.

## Что уже работает

- браузер передаёт микрофон напрямую в OpenAI по WebRTC;
- `gpt-realtime-2` отвечает голосом и допускает перебивания;
- семантическое определение конца реплики;
- живая русская расшифровка;
- замер времени от конца реплики пользователя до первого аудиофрагмента ответа;
- выбор роли, сложности и контекста кейса;
- секретный API-ключ остаётся только на сервере.

Оригинал книги Владимира Тарасова получен и хранится локально в закрытой папке `private-sources/`, исключённой из Git. До методической разметки и экспертной проверки прототип не выдаёт модельные предположения за правила автора. Технические сведения об источнике находятся в `docs/SOURCE_MANIFEST.md`.

## Запуск

1. Скопируйте `.env.example` в `.env.local`.
2. Заполните `OPENAI_API_KEY`.
3. Установите зависимости: `npm install`.
4. Запустите: `npm run dev`.
5. Откройте `http://localhost:3000` и разрешите доступ к микрофону.

## Деплой на Vercel

Добавьте `OPENAI_API_KEY` в Environment Variables проекта Vercel и выполните `vercel --prod`. Для работы микрофона нужен HTTPS; Vercel предоставляет его автоматически.

Полное проектное решение описано в [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).
